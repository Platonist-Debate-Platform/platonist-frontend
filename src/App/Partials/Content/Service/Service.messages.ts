import { defineMessages } from 'react-intl';

const MESSAGES = defineMessages({
  toServiceDetail: {
    id: 'ServicesSlider.toServiceDetail',
    defaultMessage: 'View Services',
  }
});

export default MESSAGES;